import { Router } from "express"

const route = Router()
export const usersRoutes = () => {

    route.post("", /* controller */)
    route.get("", /* controller */)
    route.get("", /* controller */)
    return route
}